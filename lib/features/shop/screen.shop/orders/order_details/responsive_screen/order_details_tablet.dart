import 'package:flutter/material.dart';

class OrderDetailsTablet extends StatelessWidget {
  const OrderDetailsTablet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}